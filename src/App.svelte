<script lang="ts">
  //import { onMount } from "svelte";
  import "./app.css";

  /*onMount(() => {
    updateTime();
    const interval = setInterval(updateTime, 1000);
    return () => clearInterval(interval);
  });*/
</script>

<main class="dashboard-container">
  <div class="searchBar">
    <input type="text" placeholder="scrape the web" />
  </div>
</main>

<style>
</style>
